CREATE TABLE person
(
    person_id   INTEGER GENERATED by default on null as IDENTITY PRIMARY KEY,
    name        VARCHAR2(20) NOT NULL,
    surname     VARCHAR2(20) NOT NULL,
    middle_name VARCHAR2(20),
    person_type VARCHAR2(20) CHECK ( person_type IN ('student', 'professor') )
);

INSERT INTO person(person_id, name, surname, middle_name, person_type)
VALUES (191950, 'Ivan', 'Ivanov', 'Ivanovich', 'student');

SELECT *
FROM person;
